{
  "name": "pos_restaurant",
  "version": "1.0.0",
  "description": "Punto de venta (POS) para un restaurante",
  "scripts": {
    "start": "tsc --build && electron src/main.js",
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@electron/remote": "^2.0.9",
    "@types/electron": "^1.6.10",
    "@types/pg": "^8.6.6",
    "electron": "^23.2.3",
    "pg": "^8.10.0",
    "typescript": "^4.9.5"
  }
}
